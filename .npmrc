# Published to public npm (registry.npmjs.org) via .github/workflows/release.yml.
# No scope override here: @arpsw/* resolves to the default public registry,
# and CI injects the auth token (NPM_TOKEN) at publish time.
